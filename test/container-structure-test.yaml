schemaVersion: 2.0.0

fileExistenceTests:
  - name: 'App directory'
    path: '/app'
    shouldExist: true
    # TODO: Ideally we dont want to run as root
    # permissions: '-rw-r--r--'
    # uid: 1000
    # gid: 1000
    isExecutableBy: 'group'

metadataTest:
  entrypoint: ['node', '/app/gomboc']
  cmd: ['-v']
