export declare enum ActionCommand {
    SCAN = "scan"
}
export declare enum ServiceCommand {
    CLOUDFORMATION = "cloudformation",
    TERRAFORM = "terraform"
}
export declare enum ClientCommand {
    GITHUB = "github",
    GITLAB = "gitlab"
}
